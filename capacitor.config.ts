import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beela303.taskmanager',
  appName: 'Task Manager',
  webDir: 'public'
};

export default config;
